
  # Portfolio Website Structure

  This is a code bundle for Portfolio Website Structure. The original project is available at https://www.figma.com/design/X89lv9H84fLqADvSwtMT1F/Portfolio-Website-Structure.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  